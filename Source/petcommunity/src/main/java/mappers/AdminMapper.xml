<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="admin">

	<select id="adminselectList" parameterType="buylistview" resultType="buylistview">
		SELECT BUYLIST_DATE, PRODUCT_NAME, BUY_CNT, BUY_TOTALPRICE, PRODUCT_ID
		FROM BUYLISTVIEW
	</select>

	<select id="getSearchDate" parameterType="map" resultType="buylistview">
		select BUYLIST_DATE, PRODUCT_NAME, BUY_CNT, BUY_TOTALPRICE, PRODUCT_ID
		from BUYLISTVIEW
		<![CDATA[
		WHERE BUYLIST_DATE >= to_date(#{startDate},'YYYY-MM-DD')
		AND BUYLIST_DATE <= to_date(#{endDate},'YYYY-MM-DD')
		]]>
	</select>


</mapper>